syntax = "proto3";

package washup.protos.notification;

message Email {
  string to = 1;
  string from = 2;
  string subject = 3;
  string template_id = 4;
  map<string, string> templateParameters = 5;
}
